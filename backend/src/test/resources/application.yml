spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: true
    defer-datasource-initialization: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 487263303071-krt3a9aao9n9hkv7vbep57gc1ggom36c.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: "http://localhost:8080/login/oauth2/code/google"
            authorization-grant-type: authorization_code
            scope: email

jwt:
  issuer: gmltjd@naver.com
  secret_key: db_projectdb_projectdb_projectdb_projectdb_projectdb_project